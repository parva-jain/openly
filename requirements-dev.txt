# Dev/test tooling (not needed at runtime). Install with:
#   .venv/bin/pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
httpx>=0.27.0   # required by FastAPI's TestClient
ruff>=0.6.0
